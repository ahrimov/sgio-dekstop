const ObjProcessor = require('./ObjProcessor');

class BoundsProcessor extends ObjProcessor {
    constructor() {
        super();
    }

    setParams(params) {
    }
}

module.exports = BoundsProcessor;
